#include <vector>

class Mergesort {
public:
	static void sort(std::vector<int>& bar, int& accesses);
	static void mergeSort(std::vector<int>& left, std::vector<int>& right, std::vector<int>& bars, int& accesses);
};